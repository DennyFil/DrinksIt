export class Bar {
    constructor(
        public id: number, 
        public address: string, 
        public city: string, 
        public country: string, 
        public name: string
        ){}
}